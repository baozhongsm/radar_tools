%--------------------------------------------------------------------------
%自动计算cfar阈值因子
%--------------------------------------------------------------------------
function alpha = cfar_alpha(pfa,N)
disp("sum(Pn) x alpha = T")
disp("calculate alpha...")
alpha = (pfa.^(-1./N)-1);
end