%--------------------------------------------------------------------------
%   自动将信号缩放到有符号AD位数 最大值
%--------------------------------------------------------------------------
function output = auto_scale(sig,AD_len)
sig = sig./max(abs(sig));
output = round(sig .* (2^(AD_len-1)-1));